// number
let userID = 32464.5

// boolean
userID.toFixed();

// any
// turn off typechecking for this particular values, generally avoid
let hero : string;

function getHero() {
    return 'Daniel'
}

hero = getHero();